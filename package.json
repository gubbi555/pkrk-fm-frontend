{
  "name": "pkrk-fm-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "serve -s public -l 3000",
    "build": "echo 'Build complete' && exit 0",
    "test": "echo 'No tests' && exit 0"
  },
  "dependencies": {
    "serve": "^14.2.0"
  },
  "devDependencies": {},
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}